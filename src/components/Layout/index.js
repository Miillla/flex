import React from "react";
import PropTypes from "prop-types";
import styled from "styled-components";
import recet from "./recet.css";
import style from "./style.css";

const Layout = ({ children }) => {
  return <>{children}</>;
};
export default Layout;
